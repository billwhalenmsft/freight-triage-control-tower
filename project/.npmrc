# @microsoft/power-apps is a PUBLIC npm package. This repo's user-level ~/.npmrc
# routes the whole @microsoft scope to GitHub Packages, which 404s here.
# Project-level config overrides user-level, so pin @microsoft to public npm.
@microsoft:registry=https://registry.npmjs.org/
